const fs = require('fs'); const { JSDOM } = require('jsdom'); const html = fs.readFileSync('index.html', 'utf8'); const dom = new JSDOM(html, { runScripts: 'dangerously', virtualConsole: new (require('jsdom').VirtualConsole)().sendTo(console) }); setTimeout(() => { const btn = dom.window.document.querySelector('.faq-question'); if(btn) { console.log('Initial Answer class:', btn.nextElementSibling.className); btn.click(); console.log('Final Answer class:', btn.nextElementSibling.className); } }, 2000);
